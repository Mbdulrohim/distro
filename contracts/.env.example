# Monad RPC endpoints — see docs/CONTRACT_SPEC.md and the monskills tooling-and-infra
# reference for supported providers. Never commit a filled-in .env.
MONAD_TESTNET_RPC_URL=
MONAD_MAINNET_RPC_URL=

# Chain IDs (reference, not secrets): testnet = 10143, mainnet = 143

# Deployment / verification — deploys go through the agent wallet + Safe multisig
# per the monskills `wallet/` skill, not a bare EOA private key. Do not add
# PRIVATE_KEY here; wire up Safe-based deployment when contract work starts.
